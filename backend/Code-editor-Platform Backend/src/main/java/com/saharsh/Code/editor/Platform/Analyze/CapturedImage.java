package com.saharsh.Code.editor.Platform.Analyze;



public class CapturedImage {
    public boolean isPhone; // true => phone image; false => laptop image
    public String image;    // base64 (or data URL if prefixed in safeDataUrl)
}
